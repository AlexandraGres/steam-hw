language: node_js
node_js:
- '14'

before_install:
- npm install -g @angular/cli
install: npm install

branches:
  only:
  - master


deploy:
  provider: heroku
  app: my-steam
  api_key:
    secure: ENCRYPTED_API_KEY
